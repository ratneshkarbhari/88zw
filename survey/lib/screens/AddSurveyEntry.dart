import 'package:flutter/material.dart';


class AddSurveyEntry extends StatelessWidget {
  


  @override

  
  Widget build(BuildContext context) {
    final Map survey = ModalRoute.of(context).settings.arguments;

    return Scaffold(

    );
  }
}